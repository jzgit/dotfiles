# ~/.tmuxinator/cov.yml
# you can make as many tabs as you wish...

project_name: TextRounds
project_root: ~/curbside/textrounds
pre: git pull origin master && git status
tabs:
  - server: rr && rails s
  - tests: rr && beg
  - console: rr && mvim
  - routes: rr && rake routes
