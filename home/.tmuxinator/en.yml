# ~/.tmuxinator/cov.yml
# you can make as many tabs as you wish...

project_name: Launchware
project_root: ~/work/new_launchware_site
pre: git pull origin master && git status
tabs:
  - server: rr && rails s
  - tests: rr && beg
  - console: rr && mvim
  - routes: rr && rake routes
