# ~/.tmuxinator/cov.yml
# you can make as many tabs as you wish...

project_name: Crowdly
project_root: ~/work/crowdly_root/Crowdly
rvm: 1.9.3-p125@crowdly
pre: git status
tabs:
  - server: rr && rails s
  - tests: rr && beg
  - console: rr && mvim
  - routes: rr && rake routes
  - kong: rr && cd ../kong
